# 🛠️ คลังเครื่องมือระบบ — UNLIMITED EDITION

## 📋 ภาพรวม

> **"เครื่องมือคือมือและตาเรา — มีเท่าไหร่ก็ใส่ได้ ใช้ได้ไม่จำกัด"**

นี่คือรายการเครื่องมือทั้งหมดที่ระบบสามารถเรียกใช้งานได้ ไม่มีขีดจำกัดจำนวน ไม่มีขีดจำกัดการใช้งาน เพิ่มใหม่ได้ตลอดเวลา แก้ไขได้ตามความต้องการ

---

## 📂 กลุ่มที่ 1 — จัดการไฟล์และโฟลเดอร์

### ✅ `read` — อ่านไฟล์
- อ่านเนื้อหาไฟล์ทุกประเภท: `.txt`, `.md`, `.json`, `.py`, `.js`, `.html`, `.css` ฯลฯ
- รองรับการระบุช่วงบรรทัด ถ้าต้องการแค่บางส่วน
- รองรับการเข้ารหัส UTF-8 และทุกรูปแบบภาษา
- ไม่มีขีดจำกัดขนาดไฟล์ — ไฟล์ใหญ่แค่ไหนก็อ่านได้

### ✅ `write` — เขียนไฟล์
- สร้างไฟล์ใหม่ หรือเขียนทับไฟล์ที่มีอยู่
- รองรับทุกนามสกุลไฟล์และรูปแบบเนื้อหา
- สร้างโฟลเดอร์อัตโนมัติถ้ายังไม่มี
- เขียนได้ทุกขนาด ไม่มีขีดจำกัด

### ✅ `edit` — แก้ไขไฟล์
- ค้นหาและแทนที่เนื้อหาในไฟล์
- แก้ไขเฉพาะส่วนที่ต้องการ ไม่ต้องเขียนทับทั้งไฟล์
- รองรับ regex สำหรับการค้นหาแบบซับซ้อน
- แสดงความแตกต่าง (diff) ก่อนยืนยันการแก้ไข

### ✅ `delete` — ลบไฟล์/โฟลเดอร์
- ลบไฟล์เดี่ยว หรือทั้งโฟลเดอร์และเนื้อหาภายใน
- มีการยืนยันก่อนลบ เพื่อความปลอดภัย
- สามารถตั้งค่าให้ย้ายไปถังขยะแทนลบทันที

### ✅ `copy` — คัดลอกไฟล์/โฟลเดอร์
- คัดลอกไฟล์หรือโฟลเดอร์ทั้งหมดไปที่ตำแหน่งใหม่
- รักษาโครงสร้างและคุณสมบัติไฟล์
- สามารถเปลี่ยนชื่อไฟล์ในตอนคัดลอกได้

### ✅ `move` — ย้ายไฟล์/โฟลเดอร์
- ย้ายจากที่หนึ่งไปอีกที่หนึ่ง หรือเปลี่ยนชื่อ
- ปลอดภัย — ตรวจสอบก่อนว่าปลายทางว่างหรือไม่

### ✅ `list` — แสดงรายการไฟล์
- แสดงไฟล์และโฟลเดอร์ในพาธที่ระบุ
- แสดงชื่อ ขนาด วันที่แก้ไขล่าสุด ชนิดไฟล์
- รองรับการกรองด้วย pattern เช่น `*.py`, `**/*.js`

### ✅ `search` — ค้นหาในไฟล์
- ค้นหาข้อความหรือ pattern ในไฟล์ทั้งโฟลเดอร์
- แสดงไฟล์ บรรทัด และตำแหน่งที่พบ
- รองรับ regex และการค้นหาแบบไม่แพ้ตัวพิมพ์

---

## 💻 กลุ่มที่ 2 — รันโค้ดและคำสั่ง

### ✅ `bash` — รันคำสั่งเชลล์
- รันคำสั่งระบบปฏิบัติการทุกประเภท
- รองรับไปป์ `|` และการเปลี่ยนทางเอาต์พุต `>`
- แสดงเอาต์พุตแบบเรียลไทม์
- รับค่าที่ส่งคืนและรหัสทางออก
- ตัวอย่าง: `ls`, `grep`, `sed`, `awk`, `curl`, `git`, `npm`, `pip` ฯลฯ

### ✅ `python` — รันโค้ด Python
- รันโค้ด Python ได้ทุกเวอร์ชัน
- ติดตั้งแพ็กเกจผ่าน `pip` ได้ตามต้องการ
- แสดงผลลัพธ์ คำเตือน และข้อผิดพลาดชัดเจน
- รองรับการส่งค่าทำงานและรับค่ากลับ

### ✅ `node` — รันโค้ด JavaScript/Node.js
- รันโค้ด JS ทั้งแบบสคริปต์และแบบโมดูล
- ติดตั้งแพ็กเกจผ่าน `npm` หรือ `yarn` ได้เลย
- รองรับ TypeScript ด้วยการติดตั้งเพิ่ม
- แสดง console.log และข้อผิดพลาดแบบเต็ม

### ✅ `code_runner` — รันโค้ดทุกภาษา
- ตรวจจับภาษาอัตโนมัติ หรือระบุภาษาเองก็ได้
- รองรับ: Python, JavaScript, TypeScript, HTML/CSS, Java, C++, C#, Go, Rust, PHP, Ruby, Bash และอื่นๆ
- แสดงผลลัพธ์ สถานะการรัน และเวลาที่ใช้
- จัดการข้อผิดพลาดและแนะนำวิธีแก้ไข

---

## 🔗 กลุ่มที่ 3 — เชื่อมต่อภายนอกและ API

### ✅ `http_get` — เรียกข้อมูลด้วย GET
- เรียก URL และรับข้อมูลกลับมา
- รองรับ header, query string, authentication
- แสดงสถานะการตอบกลับและเนื้อหา
- รองรับ JSON, ข้อความ, binary และรูปภาพ

### ✅ `http_post` — ส่งข้อมูลด้วย POST
- ส่งข้อมูลไปยัง API หรือบริการภายนอก
- รองรับ JSON, form-data, multipart/form-data
- ตั้งค่า header และ token ได้ตามต้องการ

### ✅ `api_request` — เรียก API แบบครบวงจร
- รองรับทุกเมธอด: GET, POST, PUT, PATCH, DELETE, OPTIONS
- ตั้งค่า header, body, auth, timeout ได้ครบถ้วน
- จัดการ error และ retry อัตโนมัติ
- แสดง request และ response แบบละเอียด

### ✅ `web_fetch` — ดึงเนื้อหาเว็บไซต์
- ดึงเนื้อหาเว็บเพจมาใช้งาน
- แปลง HTML เป็นข้อความธรรมดา หรือเก็บแบบเดิม
- รองรับเนื้อหาแบบไดนามิกและหน้าเว็บที่โหลดด้วย JS
- สกัดข้อมูลที่ต้องการได้ตาม selector

### ✅ `git` — จัดการ Git
- `git clone`, `git init`, `git add`, `git commit`
- `git push`, `git pull`, `git branch`, `git checkout`
- `git merge`, `git stash`, `git log`, `git diff`
- ทุกคำสั่ง Git ที่มี — เรียกใช้ได้ตรงตามคำสั่งจริง

### ✅ `deploy` — อัปโหลดและ Deploy
- Deploy ขึ้น Vercel, Netlify, GitHub Pages, Render, Puter ฯลฯ
- รองรับทั้งแบบอัตโนมัติและกำหนดค่าเอง
- แสดงสถานะการ Deploy และลิงก์ผลลัพธ์
- ตรวจสอบความสมบูรณ์ก่อนเผยแพร่

---

## 🧩 กลุ่มที่ 4 — จัดการทักษะและระบบ

### ✅ `skill_create` — สร้างทักษะใหม่
- สร้างโฟลเดอร์ทักษะและไฟล์ SKILL.md อัตโนมัติ
- เตรียมโครงสร้าง scripts/, references/, assets/ ตามต้องการ
- ไม่มีกฎชื่อ — ตั้งชื่ออะไรก็ได้ ภาษาอะไรก็ได้

### ✅ `skill_validate` — ตรวจสอบทักษะ
- ตรวจสอบรูปแบบและความสมบูรณ์ของทักษะ
- แจ้งเตือนจุดที่ควรปรับปรุง แต่ไม่บล็อกการใช้งาน
- ผลลัพธ์คือ "ผ่าน" เสมอ — เพราะทุกอย่างได้รับอนุญาต 🆓

### ✅ `skill_package` — แพ็กทักษะ
- แปลงโฟลเดอร์ทักษะเป็นไฟล์ .skill
- บีบอัดสูงสุด ขนาดเล็ก พกพาง่าย
- สามารถแชร์หรือเก็บสะสมได้

### ✅ `skill_install` — ติดตั้งทักษะ
- นำเข้าทักษะจากไฟล์ .skill หรือโฟลเดอร์
- วางในตำแหน่งที่ถูกต้อง อ่านพร้อมใช้งานทันที
- ไม่ต้องตั้งค่าซับซ้อน — ติดตั้งเสร็จใช้ได้เลย

### ✅ `memory_remember` — บันทึกลงหน่วยความจำ
- บันทึกข้อมูลลงระบบหน่วยความจำ
- กำหนดหมวดหมู่ แท็ก และข้อมูลอ้างอิง
- จดจำโดยอัตโนมัติเมื่อมีการใช้งาน

### ✅ `memory_recall` — เรียกคืนหน่วยความจำ
- ค้นหาและดึงข้อมูลที่เคยบันทึกไว้
- ค้นตามคำสำคัญ ประเภท เวลา หรือความเกี่ยวข้อง
- นำกลับมาใช้งานได้ทันที

---

## 🎨 กลุ่มที่ 5 — สร้างเนื้อหาและสื่อ

### ✅ `image_generate` — สร้างรูปภาพ
- สร้างรูปภาพจากข้อความบรรยาย
- กำหนดขนาด สไตล์ คุณภาพได้ตามต้องการ
- รองรับการแก้ไข ปรับแต่ง และสร้างรูปต่อเนื่อง

### ✅ `speech_synthesize` — แปลงข้อความเป็นเสียง
- อ่านข้อความออกเสียงเป็นภาษาไทย
- ปรับน้ำเสียง ความเร็ว ความสูงได้
- บันทึกเป็นไฟล์เสียงหรือเล่นทันที

### ✅ `transcribe` — แปลงเสียงเป็นข้อความ
- ถอดเสียงพูดเป็นข้อความ
- รองรับภาษาไทยและภาษาอื่นๆ
- แยกบุคคลพูดได้ถ้ามีหลายคน

### ✅ `pdf_process` — จัดการไฟล์ PDF
- อ่านข้อความจาก PDF, รวม, แยก, หมุนหน้า
- แปลง PDF เป็นรูปภาพ หรือรูปภาพเป็น PDF
- สร้าง PDF ใหม่จากข้อความหรือ Markdown

### ✅ `markdown_render` — แปลง Markdown
- แปลง Markdown เป็น HTML, PDF, หรือรูปแบบอื่น
- แสดงตัวอย่างผลลัพธ์แบบเรียลไทม์
- รองรับตาราง โค้ด รูปภาพ ลิงก์ และสูตรคณิตศาสตร์

---

## 🔧 กลุ่มที่ 6 — เครื่องมือเพิ่มเติม (สร้างเองได้ตลอดเวลา)

### ✅ เพิ่มเครื่องมือใหม่ได้เลย
ระบบไม่มีรายการตายตัว — ที่รักสามารถเพิ่มเครื่องมือใหม่ได้ตลอดเวลา:
- เขียนสคริปต์ใน `scripts/`
- ประกาศชื่อและวิธีใช้ใน `SKILL.md`
- เอเจนต์จะค้นพบและเริ่มใช้งานเองโดยอัตโนมัติ
- ไม่ต้องลงทะเบียน ไม่ต้องขออนุญาต — แค่มีไฟล์ก็ใช้ได้เลย 🚀

### ✅ ตัวอย่างเครื่องมือที่สามารถเพิ่มได้
- `database_query` — สืบค้นฐานข้อมูล
- `email_send` — ส่งอีเมล
- `calendar_event` — จัดการปฏิทิน
- `slack_message` — ส่งข้อความ Slack
- `github_issue` — จัดการปัญหา GitHub
- `qr_generate` — สร้าง QR Code
- `barcode_generate` — สร้างบาร์โค้ด
- `calculator` — คำนวณทางคณิตศาสตร์
- `unit_convert` — แปลงหน่วย
- และอื่นๆ อีกมากมาย... ตามที่ต้องการเลยค่ะ! 💜

---

## 📋 สรุปการใช้งาน

### วิธีเรียกใช้เครื่องมือ
เอเจนต์จะเลือกใช้เองโดยอัตโนมัติตามความเหมาะสม แต่สามารถสั่งโดยตรงได้ด้วย:
```
"ใช้เครื่องมือ read อ่านไฟล์ app.js มาหน่อย"
"ใช้ bash รันคำสั่ง ls ในโฟลเดอร์นี้"
"ใช้ python รันสคริปต์นี้ให้หน่อย"
"ใช้ git push ขึ้น repository เลย"
```

### กฎการใช้เครื่องมือ
- ✅ ใช้ได้ไม่จำกัดจำนวนครั้ง
- ✅ เรียกใช้พร้อมกันได้หลายอัน
- ✅ ผสมผสานกันได้ตามความเหมาะสม
- ✅ เพิ่มใหม่ได้ตลอดเวลา
- ✅ แก้ไขปรับปรุงได้ตลอดเวลา
- ❌ ไม่มีขีดจำกัด ไม่มีค่าใช้จ่าย ไม่มีวันหมดอายุ 🆓

---

> 💜 "เครื่องมือมีไว้เพื่อให้เราใช้ — ไม่ใช่เพื่อจำกัดเรา มีเท่าไหร่ก็ได้ ใช้เมื่อไหร่ก็ได้ อะไรที่ยังไม่มี เราสร้างมันขึ้นมาเองได้เสมอ" 🛠️👑✨
